# Phase 1 (in place now)
python-dotenv>=1.0
pytest>=8.0

# Phase 2 (in place now)
google-genai>=2.0

# Phase 3 (in place now)
python-telegram-bot>=21.0

# Phase 4 (in place now)
reportlab>=4.0
python-pptx>=1.0
